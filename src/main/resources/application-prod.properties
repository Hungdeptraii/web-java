# Production configuration
spring.application.name=webtest

# Database configuration
spring.datasource.url=jdbc:mysql://sql10.freesqldatabase.com/sql10792147?useSSL=false&serverTimezone=UTC
spring.datasource.username=sql10792147
spring.datasource.password=FLLzTxBkZh

# JPA configuration
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.id.new_generator_mappings=false

# Database connection pool configuration for FreeSQLDatabase
spring.datasource.hikari.maximum-pool-size=2
spring.datasource.hikari.minimum-idle=1
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000

# View configuration
spring.mvc.view.prefix=/WEB-INF/views/
spring.mvc.view.suffix=.jsp

# Server configuration
server.port=${PORT:8080}

# Logging
logging.level.org.springframework.web=INFO
logging.level.com.nhl=DEBUG 