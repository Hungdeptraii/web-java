spring.application.name=webtest

# JDBC URL từ Railway (quan trọng nhất)
spring.datasource.url=jdbc:mysql://shortline.proxy.rlwy.net:12992/order_doan?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC

# Thông tin đăng nhập
spring.datasource.username=root
spring.datasource.password=fgfJDWhCQkfBCvtyrnYInqRHTFPDvtQh

# Driver class
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate config
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# JSP view config (nếu dùng)
spring.mvc.view.prefix=/WEB-INF/views/
spring.mvc.view.suffix=.jsp

# File upload config (nếu cần)
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
spring.servlet.multipart.file-size-threshold=2KB

# Đọc PORT từ Railway (nếu Railway set)
server.port=${PORT:8080}
